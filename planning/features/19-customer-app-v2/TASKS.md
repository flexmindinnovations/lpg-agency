# Customer App V2 Tasks

- [ ] Define the 4 Main Tabs (Dashboard, Orders, Support, Profile).
- [ ] Setup `StatefulShellRoute` in `router.dart` and build `app_shell.dart`.
- [ ] Implement `dashboard_screen.dart` layout tweaks (add notifications icon).
- [ ] Implement `notifications_screen.dart` (Push Notifications Inbox UI).
- [ ] Implement `orders_screen.dart` (Active/Past list view).
- [ ] Implement `order_tracking_screen.dart` (Live Tracking map placeholder + milestones).
- [ ] Implement `invoice_screen.dart` (Receipt generation UI).
- [ ] Implement `support_screen.dart` (Complaints list + raise new ticket).
- [ ] Implement `profile_screen.dart` (KYC details, navigation list).
- [ ] Implement `address_management_screen.dart` (Saved addresses + Add Map pin flow).
- [ ] Implement `payment_methods_screen.dart` (Saved cards + Add method flow).
