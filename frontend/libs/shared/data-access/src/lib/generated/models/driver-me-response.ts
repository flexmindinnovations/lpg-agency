/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { DriverMeVehicle } from '../models/driver-me-vehicle';

/**
 * `GET /drivers/me` — the calling driver's own profile for the Driver
 * App's Profile tab. `name`/`phone_number` come from the linked employee;
 * `vehicle` is `null` when the driver has no active route.
 */
export interface DriverMeResponse {
  driver_id: string;
  license_expiry_date: (string | null);
  license_number: string;
  name: string;
  phone_number: string;
  status: string;
  vehicle?: (DriverMeVehicle | null);
}
