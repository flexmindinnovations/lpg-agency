/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { DriverLocationSnapshot } from '../models/driver-location-snapshot';
import { TrackingDriverInfo } from '../models/tracking-driver-info';

/**
 * Everything the customer app's order-tracking map needs beyond what it
 * already has from the order itself: the resolved route status, the driver's
 * last-known position (``None`` until the driver starts sharing), and the
 * assigned driver + vehicle.
 */
export interface OrderTrackingResponse {
  destination_label: string;
  destination_latitude: (number | null);
  destination_longitude: (number | null);
  driver?: (TrackingDriverInfo | null);
  driver_location: (DriverLocationSnapshot | null);
  order_id: string;
  route_status: (string | null);
  status: 'draft' | 'booked' | 'confirmed' | 'assigned' | 'ready_for_dispatch' | 'out_for_delivery' | 'delivered' | 'failed_delivery' | 'cancelled' | 'closed';
}
