/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RoutePageResponse } from '../../models/route-page-response';

export interface ListRoutesApiV1RoutesGet$Params {
  page?: number;
  page_size?: number;
  status?: (string | null);
  branch_id?: (string | null);
  date_from?: (string | null);
  date_to?: (string | null);
}

export function listRoutesApiV1RoutesGet(http: HttpClient, rootUrl: string, params?: ListRoutesApiV1RoutesGet$Params, context?: HttpContext): Observable<StrictHttpResponse<RoutePageResponse>> {
  const rb = new RequestBuilder(rootUrl, listRoutesApiV1RoutesGet.PATH, 'get');
  if (params) {
    rb.query('page', params.page, {});
    rb.query('page_size', params.page_size, {});
    rb.query('status', params.status, {});
    rb.query('branch_id', params.branch_id, {});
    rb.query('date_from', params.date_from, {});
    rb.query('date_to', params.date_to, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<RoutePageResponse>;
    })
  );
}

listRoutesApiV1RoutesGet.PATH = '/api/v1/routes';
