/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PlanRouteRequest } from '../../models/plan-route-request';
import { RouteResponse } from '../../models/route-response';

export interface PlanRouteApiV1RoutesPost$Params {
      body: PlanRouteRequest
}

export function planRouteApiV1RoutesPost(http: HttpClient, rootUrl: string, params: PlanRouteApiV1RoutesPost$Params, context?: HttpContext): Observable<StrictHttpResponse<RouteResponse>> {
  const rb = new RequestBuilder(rootUrl, planRouteApiV1RoutesPost.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<RouteResponse>;
    })
  );
}

planRouteApiV1RoutesPost.PATH = '/api/v1/routes';
