/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { VehiclePageResponse } from '../../models/vehicle-page-response';

export interface ListVehiclesApiV1VehiclesGet$Params {
  skip?: number;
  limit?: number;
  search?: (string | null);
  status?: (string | null);
  branch_id?: (string | null);
}

export function listVehiclesApiV1VehiclesGet(http: HttpClient, rootUrl: string, params?: ListVehiclesApiV1VehiclesGet$Params, context?: HttpContext): Observable<StrictHttpResponse<VehiclePageResponse>> {
  const rb = new RequestBuilder(rootUrl, listVehiclesApiV1VehiclesGet.PATH, 'get');
  if (params) {
    rb.query('skip', params.skip, {});
    rb.query('limit', params.limit, {});
    rb.query('search', params.search, {});
    rb.query('status', params.status, {});
    rb.query('branch_id', params.branch_id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<VehiclePageResponse>;
    })
  );
}

listVehiclesApiV1VehiclesGet.PATH = '/api/v1/vehicles';
