/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DriverResponse } from '../../models/driver-response';
import { RegisterDriverRequest } from '../../models/register-driver-request';

export interface RegisterDriverApiV1DriversPost$Params {
      body: RegisterDriverRequest
}

export function registerDriverApiV1DriversPost(http: HttpClient, rootUrl: string, params: RegisterDriverApiV1DriversPost$Params, context?: HttpContext): Observable<StrictHttpResponse<DriverResponse>> {
  const rb = new RequestBuilder(rootUrl, registerDriverApiV1DriversPost.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DriverResponse>;
    })
  );
}

registerDriverApiV1DriversPost.PATH = '/api/v1/drivers';
