/// Shared primitives used by both mobile applications.
///
/// Deliberately framework-light: no Flutter widgets, no HTTP client. Those
/// belong in `design_system` and `api_client` respectively.
library;

export 'src/result.dart';
